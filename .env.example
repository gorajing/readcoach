# Copy to .env (gitignored). Only internal tutor-version evals need any of these;
# the benchmark scoring path runs with NO credentials.
ANTHROPIC_API_KEY=        # tutor utterance layer (internal evals)
GOOGLE_API_KEY=           # judge (different model family than the tutor)
WANDB_API_KEY=            # W&B Weave tracing for internal evals only
