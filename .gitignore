# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# Env / secrets
.env
*.key
.claude/

# Data + audio + artifacts (keep large files out of git)
data/raw/
data/cache/
*.wav
*.mp3
*.flac
wandb/
.weave/
artifacts/
dist/

# OS
.DS_Store

# Blind-verify session artifacts (queue, ratings, report are per-session; not committed)
evals/results/blind_verify_queue.json
evals/results/blind_verify_ratings.csv
evals/results/blind_verify_report.json

# Label-turns session artifacts (per-session; not committed)
evals/results/label_queue.json
evals/results/turn_labels.csv

# committed test fixtures are re-included
!tests/fixtures/**
